package com.ballooner.domain.model

/** Where a newly added image should be placed relative to the comic's existing image. */
enum class ImagePosition {
    LEFT,
    RIGHT,
    TOP,
    BOTTOM,
}
